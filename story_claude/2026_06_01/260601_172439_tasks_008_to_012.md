# Conversation - Tasks 008 to 012

| Field | Value |
|---|---|
| Saved | 2026-06-01 17:24:39 |
| Commit | auto: implement todoList/history/0601 tasks 008-012 |
| Project | C:\project\vLearn2 |

---

## User

do the task from 008-012 in todoList/history/0601.
After you have done task, produce report what you have done and save as md format to "todoList_report\history/0601" folder.
md filename can be xxx.md where xxx means the current todo file name.

## Assistant

Explored the codebase (backend NestJS entities, admin panel Next.js routes, Flutter app) and implemented all five tasks:

**Task 008 – Field-level encryption for sensitive DB columns**
- Existing `backend/src/common/field-encryption.ts` (AES-256-CBC deterministic) was already applied to `UserEntity.name`, `license_serial`, `license_machine_id`, and `UserInfoEntity.email`.
- Added `encryptedFieldTransformer` to `UserInfoEntity.suspended_reason`.
- Fixed admin user search: email is now exact-matched using `encryptField()` instead of `ILIKE` (partial match is incompatible with encryption).
- Added `DB_FIELD_ENCRYPTION_KEY` to `.env` with a dev placeholder.
- Created `backend/src/common/encryption/encrypted-fields.ts` as a canonical registry listing all five encrypted fields with their table/column/entity names.

**Task 009 – User image upload**
- Backend `POST /users/avatar` and Flutter `edit_profile_dialog.dart` already implement upload.
- Added `avatar_url` and `avatar_emoji` to the `GET /admin/users/:id` response.
- Added avatar display (circular image or emoji fallback) at the top of the admin panel `UserDetailsModal`.

**Task 010 – Rename /personas/ → /teachers/ + gender-based TTS**
- Backend: renamed `@Controller('personas')` → `'teachers'` and `@Controller('admin/personas')` → `'admin/teachers'`.
- Flutter: updated `PersonasApi` to call `/teachers` and `/teachers/:id`.
- Admin panel: renamed folder `personas/` → `teachers/`, updated nav label to "Teachers", rewrote all internal links and query keys to use `/admin/teachers`.
- Gender field (`female`/`male`/`neutral`) was already in `PersonaEntity`.
- Added `_pickVoiceForGender()` to `_TutorModeViewState`: when `persona.voiceId` is null, selects the first available voice whose name matches known female/male name fragments (amy, jenny, alan, james, etc.) before falling back to index 0.

**Task 011 – docs/combinestt switching guide**
- Created `docs/combinestt/README.md` with:
  - Overview of the current sherpa-onnx architecture and abstract interfaces.
  - Skeleton implementations of `NativeSttService` / `NativeTtsService` wrapping MethodChannel/EventChannel.
  - Step-by-step instructions to swap the Riverpod provider in `main.dart`.
  - A `FallbackSttService` pattern for combining both implementations with graceful fallback.
  - Checklist and file map.

**Task 012 – App usage analytics + user report function + admin toggle**
- New entity `UserReportEntity` (`vl_user_reports`) with fields: user_id, type, content, platform, app_version, created_at.
- Backend: `POST /users/report` endpoint in `UsersController`; registered entity in `UsersModule`.
- Backend: Extended `AdminStatsController` with:
  - `GET /admin/stats/usage` — sessions per platform (joined to users) + network bytes per platform.
  - `GET /admin/stats/reports` — paginated list of user reports (filtered by type).
- Registered `UserReportEntity` in `AdminModule`.
- App-config seed: added `system.user_report_enabled` (visible to app) and `system.usage_analytics_enabled`.
- Flutter: added `sendReport()` to `UsersApi`; added "Send feedback" tile to `SettingsScreen` gated by `system.user_report_enabled` flag; inline report dialog with type selector (Feedback / Bug / Other) and message field.
- Admin panel: new `usage/page.tsx` (sessions + network breakdown by platform) and `reports/page.tsx` (paginated report list with inline expand); both added to sidebar nav under `analytics.view` permission.

---

## Prompt

do the task from 008-012 in todoList/history/0601.
After you have done task, produce report what you have done and save as md format to "todoList_report\history/0601" folder.
md filename can be xxx.md where xxx means the current todo file name.
