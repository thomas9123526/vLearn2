# Public — exposed to the browser bundle via Next.js.
# Relative to the site origin so it works from remote browsers; nginx
# proxies /vfls/* → http://127.0.0.1:5100/api/* (see app.conf).
NEXT_PUBLIC_API_BASE_URL=/vfls

# Server-only (used by the BFF proxy in next.config.mjs)
BACKEND_BASE_URL=http://localhost:5100
