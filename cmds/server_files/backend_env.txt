# Generated by Claude for LM Studio offline AI provider.
# This file is gitignored. Edit freely — only .env.example is committed.

# ─── Server ─────────────────────────────────────────────────
NODE_ENV=development
PORT=5100
PUBLIC_BASE_URL=http://localhost:5100
CORS_ORIGINS=http://localhost:5173,http://localhost:4100

# ─── Database (PostgreSQL) ──────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vlearn2
DB_USER=myuser
DB_PASSWORD=mypassword

# ─── Auth (JWT) ─────────────────────────────────────────────
# CHANGE BEFORE EVER EXPOSING THIS BOX TO THE INTERNET.
# Generate with: openssl rand -base64 48
JWT_ACCESS_SECRET=dev-only-change-me-minimum-32-characters-long-secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_SECRET=dev-only-change-me-different-min-32-characters-long
JWT_REFRESH_EXPIRES=7d

# ─── AI Provider — LM Studio (offline) ──────────────────────
# Run LM Studio → Developer tab → load gpt-oss-20b → Start Server.
# If you swap to a different model, update AI_CHAT_MODEL / AI_ANALYSIS_MODEL
# to the exact id LM Studio shows for the loaded model.
AI_PROVIDER=openai-compatible
AI_CHAT_MODEL=ggml-org/gemma-3-1b-it-GGUF
AI_ANALYSIS_MODEL=ggml-org/gemma-3-1b-it-GGUF
ANTHROPIC_API_KEY=

OPENAI_BASE_URL=http://127.0.0.1:8080/v1
OPENAI_API_KEY=not-needed
AI_PROVIDER_LABEL=llamacpp

# ─── Content Guard & Compression ────────────────────────────
GZIP_ENABLED=true
GZIP_THRESHOLD_BYTES=102400

# ─── File Storage ───────────────────────────────────────────
STORAGE_PROVIDER=local
LOCAL_UPLOAD_DIR=./uploads
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_PUBLIC_BASE_URL=

# ─── Rate Limiting ──────────────────────────────────────────
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=120
MAX_AI_MESSAGES_PER_DAY=100
