# Public — exposed to the browser bundle via Next.js
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Server-only (used by the BFF proxy in next.config.mjs)
BACKEND_BASE_URL=http://localhost:3000
