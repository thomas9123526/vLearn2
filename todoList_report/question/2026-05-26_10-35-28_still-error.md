# still error

Session: `c8b9bc59-8e0b-457d-a3e0-6557fcbf5b50`
Saved: 2026-05-26T01:35:28.036Z

## User

still error

## Assistant

The defensive guards only cover `_onRiveInit` and `_applyMoodToRive`. If the throw is happening elsewhere — inside the `RiveAnimation.asset` widget's internal parse/render, or in the Rive `onTick` loop — my try/catch never sees it.

I need the **full stack trace** to fix it precisely. The single-line error you pasted tells me _what_ but not _where_.

Can you paste the lines that appear right after `RangeError (length): Invalid value: Not in inclusive range 0..1: 2`? It should look like:

```
#0      _SomeClass.method (package:rive/src/...)
#1      OtherClass.something (package:rive/...)
#2      ...
```

That tells me whether to:
- Strengthen `_riveAssetUsable` (if it's a parse-time throw),
- Bump `rive` to `0.14.7` (if 0.13 simply can't handle this `.riv` format),
- Or strip the state-machine wiring entirely and play the default timeline only (if the throw is in the runtime's state-machine evaluator).

While you grab the trace, one quick sanity check you can also try: in Rive Editor, **File → Export → For Runtime**, and if there's a "Runtime version" dropdown, pick the oldest available — then drop the new `tutor_hiro.riv` into `flutter_app/assets/animations/` and rebuild.
