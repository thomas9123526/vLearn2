# continue

Session: `d5a91a17-a96b-414b-9652-b5d2d7c4c94f`
Saved: 2026-06-02T14:17:48.967Z

## User

continue

## Assistant

Now commit with story:

## Assistant

Done. Full coverage now:

| Check | Trigger |
|---|---|
| Gradle transforms `metadata.bin` | Always — zero-byte = delete |
| Gradle stale `.lock` files | Always — skipped if Java running |
| Gradle incomplete downloads | Always — `.part` / `.tmp` |
| Gradle wrapper zip | Always — zip without extraction marker |
| **Gradle build-cache** | `-Nuclear` only — wipes incremental results |
| Pub package dirs | Always — missing/zero-byte `pubspec.yaml` |
| Pub zero-byte source files | Always |
| Pub hash files | Always |
| **Flutter engine cache** | Always — checks stamp + zero-byte artifacts |
| **Project `.dart_tool`** | Always — validates `package_config.json` |

Auto-runs `flutter pub cache repair` if pub issues found, and `flutter pub get` if `.dart_tool` was corrupt.
