# hi

Session: `f56bdf13-4b13-4f2d-9e88-40dba40b3f91`
Saved: 2026-06-02T16:37:07.540Z

## User

hi

## Assistant

The crash is in `ASProxy64.dll` — a third-party DLL being injected into the Java process. Let me read the crash log for more details.

## Assistant

The crash is `ASProxy64.dll` — this is **AhnLab** security software (Korean antivirus) injecting a proxy DLL into Java and crashing it. It hooks into Windows I/O calls, and Java 21's new `WEPoll` selector (used by Gradle's socket connection) triggers the crash.

Let me check `gradle.properties` to add a JVM flag that avoids the WEPoll code path:

## Assistant

The fix: force Java to use the old Windows selector instead of WEPoll by adding `-Djava.nio.channels.spi.SelectorProvider=sun.nio.ch.WindowsSelectorProvider` to the JVM args:

## Assistant

Now commit and then try the build:

## Assistant

**Root cause:** AhnLab (안랩) security software injects `ASProxy64.dll` into every process and hooks Windows network I/O. Java 21 introduced a new `WEPoll`-based selector — AhnLab's hook crashes it with an illegal instruction when Gradle opens a socket to the daemon.

**Fix:** `gradle.properties` now forces the old Windows selector:
```
-Djava.nio.channels.spi.SelectorProvider=sun.nio.ch.WindowsSelectorProvider
```

Try running `flutter build apk --debug` again — the Gradle daemon should start cleanly now.
