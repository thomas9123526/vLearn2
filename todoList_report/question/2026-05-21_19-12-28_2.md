# 2

Session: `32f06577-e762-43c8-82ca-6f703e46a711`
Saved: 2026-05-21T10:12:28.637Z

## User

2

## Assistant

Steps for you:

1. Download `zlib-1.3.1.tar.gz` from **<https://zlib.net/>** (~600 KB).
2. Extract it. You'll get a folder named `zlib-1.3.1/` containing `zlib.h`, `deflate.c`, `inflate.c`, etc., plus a `CMakeLists.txt`.
3. Move that folder so its **contents** land at `c:\project\vLearn2\datamanage\vendor\zlib\` — i.e. the result should be:

```
datamanage/vendor/zlib/
├── CMakeLists.txt
├── zlib.h
├── zconf.h
├── deflate.c
├── inflate.c
├── ... (rest of the zlib source files)
└── LICENSE  (or note in README)
```

Either move the *contents* of `zlib-1.3.1/` into `vendor/zlib/`, or rename `zlib-1.3.1/` to `zlib/` and place at `vendor/zlib/` — same result.

4. Tell me when done. I'll verify the layout, wire up CMake to build zlib as a static lib, and proceed with Stage 4 (compression in the packer).
