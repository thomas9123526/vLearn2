# again

Session: `1eb40659-e5c9-4cd9-aa44-b57f47d3b5f7`
Saved: 2026-05-27T10:12:04.370Z

## User

again

## Assistant

The logic in [app_config.dart:55](flutter_app/lib/core/config/app_config.dart#L55):

```dart
resolvedModel = (!p.isAbsolute(trimmed) && configDir != null)
    ? p.join(configDir, trimmed)
    : trimmed;
```

If the path **is absolute** — `p.isAbsolute(trimmed)` is `true`, so `!p.isAbsolute` is `false`, the condition short-circuits, and `trimmed` is used **as-is**. No joining happens. The old behavior is preserved exactly.

Examples:

| `"model"` value in JSON | Result |
|---|---|
| `"data/sherpa_2023"` | `/storage/emulated/0/룡마/가상외국어회화/data/sherpa_2023` |
| `/storage/emulated/0/룡마/가상외국어회화/data/sherpa_2023` | `/storage/emulated/0/룡마/가상외국어회화/data/sherpa_2023` |
| `/mnt/sdcard/룡마/가상외국어회화/data/sherpa_2023` | `/mnt/sdcard/룡마/가상외국어회화/data/sherpa_2023` |

So absolute paths work exactly as before — no change in behavior.
