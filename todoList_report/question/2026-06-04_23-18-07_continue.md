# continue

Session: `ec7e9529-1399-4642-b1c1-753f53834cc7`
Saved: 2026-06-04T14:18:07.689Z

## User

continue

## Assistant

Here's the complete map of the evaluation pipeline:

---

**Step 1 — Build the evaluation system prompt**
[prompt-builder.service.ts:128](backend/src/ai/prompt-builder.service.ts#L128) — `buildEvaluationSystemPrompt()`
- Loads the `evaluation_system` template from DB (admin-editable in admin panel → Prompts tab)
- Falls back to `DEFAULT_EVALUATION_SYSTEM` at [line 339](backend/src/ai/prompt-builder.service.ts#L339) if no DB template
- Replaces `{country_adjective}`, `{learner_description}`, `{avoid_cultures_phrase}` from `vl_prompt_vars`
- Appends `/think` at the end (tells Qwen3 to output a reasoning block)

**Step 2 — Build the user message (transcript)**
[conversation.orchestrator.ts:210](backend/src/ai/conversation.orchestrator.ts#L210) — inside `evaluateSession()`
- Formats messages as `[USER turn 0] … / [TUTOR] …`
- Builds the header: `Target CEFR level`, `Tutor role`, `Learner role`, `Assigned topic`, `Assigned subtopics`
- This matches the format in your `eval_example.txt` exactly

**Step 3 — Call the AI**
[conversation.orchestrator.ts:263](backend/src/ai/conversation.orchestrator.ts#L263)
- `this.ai.chat(...)` with `maxTokens: 1500, temperature: 0.3`
- Goes through the OpenAI-compatible provider → `POST /v1/chat/completions` → your mock server

**Step 4 — Parse the response**
[conversation.orchestrator.ts:286](backend/src/ai/conversation.orchestrator.ts#L286)
- Strips `<think>…</think>` block
- Any text between `</think>` and the first `{` becomes `session_feedback`
- Finds the outermost `{…}` and `JSON.parse()`s it into `EvaluationResult`
- Returns `null` if no JSON found (caller skips silently)

So in the **mock server**, for evaluation calls the response just needs to be either:
- Pure JSON (if `AI_DISABLE_THINKING=true` behavior — what the mock currently does), or
- `<think>reasoning here</think>` + JSON (the orchestrator strips the think block either way)

Both work. The current mock strips thinking and returns clean JSON, which is the safe path.
