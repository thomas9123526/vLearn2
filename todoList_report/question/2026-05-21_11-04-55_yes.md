# yes

Session: `32f06577-e762-43c8-82ca-6f703e46a711`
Saved: 2026-05-21T02:04:55.948Z

## User

yes

## Assistant

Applying the four optimizations now.

## Assistant

Now the inline-`Opacity` rewrites for the centerpiece pieces.

## Assistant

Now `_TapToBeginButton` — same pattern, but it has hover state and the button body is more elaborate.

## Assistant

Now verify with `flutter analyze`.

## Assistant

Clean. Committing per the auto-commit memory rule.

## Assistant

Committed `7ef27fe`. Summary of what landed:

- **`_FloatingGlyph` → StatefulWidget** with `late final Widget _svg` so the SVG XML is parsed once instead of ~60×/sec per glyph. Bob displacement moved into `Transform.translate` so the parent layout isn't re-walked.
- **RepaintBoundary around each glyph** so the bob's repaints stay local and don't dirty the wordmark/monogram/slogan.
- **All inline `Opacity` widgets gone** — replaced with `FadeTransition` (and `ScaleTransition` on the monogram). No more `saveLayer` per animated element. `_Wordmark`, `_Slogan`, `_TapToBeginButton` pass their heavy static subtree (RichText / Text / Material pill) as `child:` so `AnimatedBuilder` only rebuilds the `Transform.translate` wrapper.
- **`_bob.stop()` before `context.go`** so the route transition isn't competing with 8 per-frame transforms.

`flutter analyze` clean on the file. Same choreography and visuals — just much cheaper per frame.

If you want to push further later, the next two tiers are: `flutter_native_splash` (kills the native-splash → black → Flutter-splash flash on cold start) and dropping a couple of mobile glyphs to lighten the perpetual bob. But the wiring fixes alone should move the needle the most.
