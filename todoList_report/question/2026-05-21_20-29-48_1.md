# 1

Session: `32f06577-e762-43c8-82ca-6f703e46a711`
Saved: 2026-05-21T11:29:48.388Z

## User

1

## Assistant

OpenSSL via Git for Windows — got it. Writing the scripts now.

## Assistant

OpenSSL 3.5.6 confirmed. Building the CA structure.

## Assistant

Update `.gitignore` to allow scripts but block keys/certs.

## Assistant

Now actually test both scripts end-to-end.

## Assistant

Root CA generated. Now an admin sub-CA.

## Assistant

PowerShell 5.1 doesn't read UTF-8 without BOM — the em-dashes in `issue_admin_ca.ps1` got mis-decoded and broke the parser. Replacing them with ASCII hyphens.

## Assistant

Both scripts work — chain verified. Quick double-check that `-Force` is required to overwrite.

## Assistant

Scripts + README are tracked, `issued/` is properly gitignored. Update README + commit.

## Assistant

Committed `7439571`. **Stage 5 done.**

## What's now in the repo

```
datamanage/ca/
├── README.md                    workflow + compromise model + security checklist
├── make_root_ca.ps1             one-time root CA generator
├── issue_admin_ca.ps1           per-admin sub-CA issuer
└── issued/                      ← gitignored (your generated keys + certs)
    ├── root/
    │   ├── root_ca.key          ← OFFLINE this. Never commit.
    │   └── root_ca.crt          ← pin its pubkey in Flutter app (Stage 8)
    └── admins/alice/
        ├── admin.key            ← admin's signing key (config.json: signing.key_path)
        └── admin.crt            ← admin's cert (config.json: signing.cert_path)
```

## Verified on this box

| Action | Result |
| --- | --- |
| `make_root_ca.ps1` | root cert issued, SHA-256 fingerprint `BE:A7:31:D0:…:67:9C:1C` |
| `issue_admin_ca.ps1 -Name alice` | alice cert issued |
| `openssl verify -CAfile root_ca.crt admin.crt` | `OK` |
| Re-running both without `-Force` / manual delete | refused (safety guards work) |
| `git check-ignore` on `issued/root/root_ca.key` | excluded by `.gitignore: ca/issued/` |

## Notes for your real deployment

You ran the scripts against a test root + admin. Before shipping for real:

1. **Delete `ca/issued/`** (or move it offline).
2. **Re-run `make_root_ca.ps1`** on a machine that's normally offline — laptop you set aside, or VM that you snapshot and shut down.
3. **Move `root_ca.key` off** that machine to a locked drawer / hardware token. Keep `root_ca.crt` accessible.
4. **Run `issue_admin_ca.ps1 -Name <you>`** for yourself. The admin key + cert here are what `config.json.signing` will reference in Stage 6.

## Stage 6 next

Wire signing into the packer:

- Read admin's `signing.cert_path` + `signing.key_path` from config.
- After writing `[Header][Manifest][Data]`, append the admin's cert as section [4].
- Compute SHA-256 over `header ‖ manifest ‖ data ‖ cert`.
- Sign with admin's P-256 key via Windows CNG (`BCRYPT_ECDSA_P256_ALGORITHM` — same API family that does our SHA-256 already).
- Append signature as section [5].
- Patch the four offsets/lengths into the header.

Same approach as Stage 3 — no new vendored library. mbedTLS still lands in Stage 7 for AES-GCM.

Go Stage 6?
