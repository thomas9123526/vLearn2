# CONTINOUE

Session: `b7a491a3-7a1e-4010-afd8-869021ec266b`
Saved: 2026-06-01T09:31:47.651Z

## User

CONTINOUE

## Assistant

Good. Now check and fix the teachers page — it also has `overflow-hidden` on the Card:

## Assistant

Teachers page has no dropdown — it uses direct links. The fix is only needed in the users page. Let me commit:

## Assistant

Fixed. The cause was `<Card className="overflow-hidden">` — the `overflow: hidden` clips any child that renders outside the card boundary, including the absolute-positioned dropdown.

The fix renders the dropdown via `createPortal` directly into `document.body` using `position: fixed` with coordinates from `getBoundingClientRect()`, so it's completely outside the overflow constraint. All menu items (View details, Reset password, Block/Restore) will now be fully visible and clickable regardless of where the row sits on the page.
