# Option A

Session: `1eb40659-e5c9-4cd9-aa44-b57f47d3b5f7`
Saved: 2026-05-27T02:33:24.846Z

## User

Option A

## Assistant

Clone done. Bootstrap next:

## Assistant

Now installing OpenSSL. This is the slow step (~10 min — it builds from source). Running in background and notifying when done:

## Assistant

Vcpkg is now building OpenSSL from source in the background. This typically takes 8–15 minutes on a fresh box (it compiles the full openssl tree with MSVC plus a handful of transitive dependencies). I'll be notified when it finishes and will then set `OPENSSL_ROOT_DIR` and confirm the headers are in place.
