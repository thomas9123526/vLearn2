# ok

Session: `1eb40659-e5c9-4cd9-aa44-b57f47d3b5f7`
Saved: 2026-05-27T02:52:43.455Z

## User

ok

## Assistant

Done. After each build, `libcrypto-3-x64.dll` will be copied next to `KeyGenVS2022.exe` so it launches without needing the vcpkg bin folder on `PATH`. Release pulls from `$(OPENSSL_ROOT_DIR)\bin`, Debug from `$(OPENSSL_ROOT_DIR)\debug\bin`. Committed as `1ca9ae8`.

Restart VS to pick up the new `OPENSSL_ROOT_DIR`, then rebuild — should compile and run cleanly now.
