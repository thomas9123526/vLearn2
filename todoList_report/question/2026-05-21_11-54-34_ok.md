# ok

Session: `32f06577-e762-43c8-82ca-6f703e46a711`
Saved: 2026-05-21T02:54:34.691Z

## User

ok

## Assistant

Applying #4 now and setting up #2.

## Assistant

Now check Flutter version (for Impeller default) and the pubspec / theme surface colors needed for `flutter_native_splash`.

## Assistant

Flutter 3.41.9 — Impeller is default on Android since 3.27, so that's already on. Need theme surface colors for the native splash.

## Assistant

Surgical findings:
- Dark mode users currently see a **white flash** on cold start (both `LaunchTheme` styles point to the same `drawable/launch_background.xml` which is hardcoded white).
- No Android 12+ splash API attributes set.

Adding `flutter_native_splash` to handle both. It generates all the right XML (drawables per density, Android-12 splash API attrs, dark variants) from one config block in `pubspec.yaml`. No manual XML editing.

## Assistant

Now run pub get + the generator.

## Assistant

`windowSplashScreenBackground` (Android 12+ API) is now set, and the missing `drawable-night/launch_background.xml` was created. Verify nothing else broke.

## Assistant

Committed `9d24f95`. What landed:

**Timing cut (returning users: 3.5s → 2.0s of wait)**
- `_entrance` controller 1500ms → 900ms.
- `_autoNavDelay` 2000ms → 1100ms.
- All per-element `Interval`s scale proportionally — same animation, no visual regression.

**Native splash bridge via `flutter_native_splash`**
- Added dep + config (color-only, light/dark surface colors that match the Flutter splash).
- Generator created the previously missing **`drawable-night/launch_background.xml`** — this is what was causing the white flash on dark-mode cold start.
- Generator created **Android 12+ SplashScreen API styles** (`values-v31`, `values-night-v31`) with `windowSplashScreenBackground` so the OS-mandated splash on modern Android matches the Flutter splash background instead of falling back to the system default.

**Impeller check**
- Already default on Android in your Flutter 3.41.9 — no flag needed.

## How to validate

Run in release mode on Android — that's where the perceptual win is biggest:

```powershell
cd flutter_app
flutter run --release
```

You should see: the surface color appears the moment you tap the icon (no white flash on dark mode), Flutter splash starts on top of that color (no visible seam), entrance finishes in ~900ms, navigation to sign-in in ~1.1s for returning users.

## If it still feels slow

The remaining levers from the recommendation list are:
- **Pre-rasterize the 9 SVGs to PNG** — biggest remaining per-frame win, but requires baking out icons.
- **Add a centered monogram PNG to the native splash config** — perception win; needs a 384×384 export of the badge.
- **Prefetch `_resolveReturning` in `main.dart`** — saves ~50ms first-frame.

Say the word on any of these.
