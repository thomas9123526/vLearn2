# Port this proxy listens on
PORT=4099

# The real model URL (llama.cpp / Qwen / any OpenAI-compatible server)
UPSTREAM_URL=http://localhost:1234

# Optional: truncate long message content in the console (chars, 0 = no limit)
MAX_CONTENT_CHARS=800
